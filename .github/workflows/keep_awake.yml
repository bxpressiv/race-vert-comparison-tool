name: Wake Up App
on:
  schedule:
    - cron: '0 */6 * * *' 
  workflow_dispatch:      

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send HTTP Request
        run: curl -I https://race-vert-comparison-by-mkultrarun.streamlit.app/
